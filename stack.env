# Stack environment variables for Docker deployment
# This file is gitignored and should contain production secrets

# Database password (change this in production!)
POSTGRES_PASSWORD=password

# SECURITY: Generate with 'openssl rand -base64 48' - MUST be 32+ chars
JWT_SECRET=CHANGE_ME_GENERATE_WITH_openssl_rand_base64_48

# CRITICAL: Generate with 'openssl rand -hex 32' - NEVER change after deployment
# Changing this will permanently brick all stored SMTP/API credentials
ENCRYPTION_KEY=CHANGE_ME_GENERATE_WITH_openssl_rand_hex_32

# Branding & URLs (configure for your deployment)
APP_NAME=OverSeek
APP_URL=https://overseek.plateit.au
API_URL=https://api.overseek.plateit.au
CONTACT_EMAIL=notifications@overseek.plateit.au

# CORS & Allowed Hosts
CLIENT_URL=https://overseek.plateit.au
CORS_ORIGIN=https://overseek.plateit.au
CORS_ORIGINS=https://overseek.plateit.au
ALLOWED_HOSTS=overseek.plateit.au,localhost
