# OverSeek Stack Environment Configuration
# Copy this file to stack.env and customize for your deployment
# cp stack.env.example stack.env

# =============================================================================
# REQUIRED - You must set these
# =============================================================================

# Database password (CHANGE THIS in production!)
POSTGRES_PASSWORD=changeme_in_production

# JWT secret for authentication (generate with: openssl rand -hex 32)
JWT_SECRET=generate_a_secure_random_string_here

# Encryption key for sensitive data (generate with: openssl rand -hex 32)
# Used to encrypt SMTP passwords, HTTP relay API keys, and other secrets
# WARNING: Must remain constant across deployments! Changing this will make
# all previously encrypted data (email configs, relay settings) unreadable.
ENCRYPTION_KEY=generate_a_secure_random_string_here

# =============================================================================
# Branding & URLs - Configure for your deployment
# =============================================================================

# Your application name (shown in emails, 2FA apps, AI prompts)
APP_NAME=OverSeek

# Your frontend URL (where users access the dashboard)
APP_URL=http://localhost:5173

# Your API URL (where the backend runs)
API_URL=http://localhost:3000

# Contact email for notifications (used in push notification VAPID)
CONTACT_EMAIL=notifications@localhost

# =============================================================================
# CORS & Security
# =============================================================================

# Frontend URL for CORS (usually same as APP_URL)
CLIENT_URL=http://localhost:5173
CORS_ORIGIN=http://localhost:5173
CORS_ORIGINS=http://localhost:5173

# Allowed hostnames (comma-separated)
ALLOWED_HOSTS=localhost

# =============================================================================
# Advanced - Elasticsearch tuning (optional)
# =============================================================================

# Elasticsearch heap size (default: 512m min, 1g max)
# ES_HEAP_MIN=512m
# ES_HEAP_MAX=1g

# =============================================================================
# Frontend Branding (VITE_* - exposed to client)
# =============================================================================

# Application name shown in legal pages, UI elements
VITE_APP_NAME=OverSeek

# Contact emails for legal pages (Terms, Privacy, Data Deletion)
VITE_SUPPORT_EMAIL=support@yourdomain.com
VITE_LEGAL_EMAIL=legal@yourdomain.com
VITE_PRIVACY_EMAIL=privacy@yourdomain.com

# API URL for frontend (usually same as API_URL)
# VITE_API_URL=http://localhost:3000

# =============================================================================
# Note: API keys and integrations are configured in the app
# =============================================================================
# - OpenRouter API Key: Settings > Intelligence (per-account)
# - Google Ads: Settings > Integrations > Google Ads (OAuth flow)
# - Meta Ads: Settings > Integrations > Meta Ads (OAuth flow)
# - Platform SMTP: Super Admin > Platform Settings
